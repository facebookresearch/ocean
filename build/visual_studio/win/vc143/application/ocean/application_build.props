<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="Current" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <_PropertySheetDisplayName>Build Application</_PropertySheetDisplayName>
  </PropertyGroup>

  <PropertyGroup>
    <ocean_application_build_imported>True</ocean_application_build_imported>
  </PropertyGroup>

  <ImportGroup Label="Dependencies">
    <Import Condition="'$(ocean_common_imported)' != 'True'" Project="..\..\ocean_common.props"/>
  </ImportGroup>

  <!-- Debugger environment for Shared builds to find DLLs -->
  <!-- OceanSharedLibPaths accumulates DLL directories; library _common.props files append their own 3rdparty paths -->
  <PropertyGroup Condition="$(Configuration.StartsWith('Shared'))">
    <OceanSharedLibPaths>$(OceanRootPath)bin\lib\$(OceanConfigFolder)</OceanSharedLibPaths>
    <LocalDebuggerEnvironment>PATH=$(OceanSharedLibPaths);%PATH%</LocalDebuggerEnvironment>
    <DebuggerFlavor>WindowsLocalDebugger</DebuggerFlavor>
  </PropertyGroup>
</Project>
