<Project>
  <PropertyGroup>
    <!-- Ocean root path (relative to this file: frameinspector -> vc143 -> extensions -> visual_studio -> build -> ocean) -->
    <OceanRootPath>$([MSBuild]::NormalizeDirectory('$(MSBuildThisFileDirectory)..\..\..\..\..'))</OceanRootPath>
  </PropertyGroup>

  <PropertyGroup>
    <!-- Lowercase configuration folder (debug/release) -->
    <ConfigurationLower>$(Configuration.ToLower())</ConfigurationLower>

    <!-- Base paths -->
    <ExtensionOutputRoot>$(OceanRootPath)bin\extensions\frameinspector\</ExtensionOutputRoot>
    <ExtensionIntermediateRoot>$(OceanRootPath)bin\tmp\extensions\frameinspector\</ExtensionIntermediateRoot>

    <!-- Default: All projects output to tmp folder (treated as intermediate) -->
    <!-- The VSIX project (FrameInspector) overrides this to the final output location -->
    <OutputPath>$(ExtensionIntermediateRoot)$(MSBuildProjectName)\$(ConfigurationLower)\</OutputPath>
    <OutDir>$(OutputPath)</OutDir>
    <IntermediateOutputPath>$(ExtensionIntermediateRoot)$(MSBuildProjectName)\$(ConfigurationLower)\</IntermediateOutputPath>
    <IntDir>$(IntermediateOutputPath)</IntDir>
    <BaseIntermediateOutputPath>$(IntermediateOutputPath)</BaseIntermediateOutputPath>
  </PropertyGroup>

  <!-- SDK-style project settings -->
  <PropertyGroup Condition="'$(UsingMicrosoftNETSdk)' == 'true'">
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
    <WarningLevel>4</WarningLevel>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>
</Project>
