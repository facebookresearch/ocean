<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="Current" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <_PropertySheetDisplayName>Use Ocean Devices MapBuilding JNI (Android)</_PropertySheetDisplayName>
  </PropertyGroup>

  <PropertyGroup>
    <ocean_devicesmapbuildingjni_use_imported>True</ocean_devicesmapbuildingjni_use_imported>
  </PropertyGroup>

  <ImportGroup Label="Dependencies">
    <Import Condition="'$(ocean_devicesmapbuildingjni_common_imported)' != 'True'" Project="devicesmapbuildingjni_common.props"/>
  </ImportGroup>

  <!-- Link against debug JNI library with whole-archive (force static linking) -->
  <ItemDefinitionGroup Condition="$(Configuration.Contains('Debug'))">
    <Link>
      <AdditionalOptions>-Wl,--whole-archive -Wl,-Bstatic -lOceanDevicesMapBuildingJniD -Wl,-Bdynamic -Wl,--no-whole-archive %(AdditionalOptions)</AdditionalOptions>
    </Link>
  </ItemDefinitionGroup>

  <!-- Link against release JNI library with whole-archive (force static linking) -->
  <ItemDefinitionGroup Condition="!$(Configuration.Contains('Debug'))">
    <Link>
      <AdditionalOptions>-Wl,--whole-archive -Wl,-Bstatic -lOceanDevicesMapBuildingJni -Wl,-Bdynamic -Wl,--no-whole-archive %(AdditionalOptions)</AdditionalOptions>
    </Link>
  </ItemDefinitionGroup>
</Project>
